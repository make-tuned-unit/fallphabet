export default function ShareScore({ score, chain, wordsUsed }) {
  // Placeholder: show modal and copy to clipboard
  alert(`Score: ${score} pts\nChain Max: x${chain}\nWords Used: ${wordsUsed}`);
}

